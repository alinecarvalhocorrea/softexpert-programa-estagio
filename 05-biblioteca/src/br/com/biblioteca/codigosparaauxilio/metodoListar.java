package br.com.biblioteca.codigosparaauxilio;

//Banco de Livros
public class metodoListar {

	/*public Set<Livro> listar() {
		return banco;
		
		// List<String> listaOrdenadaDeLivros = new ArrayList<>();
		// List<Livro> livrosOrdenados = new ArrayList<>(banco);

		// Collections.sort(banco, new OrdenarLivroPorResumo());

		// return livrosOrdenados;
		// return banco;

		
		 Iterator<Livro> iterator = banco.iterator(); int contador = 0;
		 
		 * while (iterator.hasNext()) { contador += 1; Livro livro =
		 * iterator.next(); String livroTitulo = livro.getTitulo(); String
		 * livroAutor = livro.getAutor(); String livroCategoria =
		 * livro.getCategoria(); String livroLocal = livro.getLocal(); String
		 * informacoesDoLivro = "\n " + contador + " - " + " TÍTULO: '" +
		 * livroTitulo + "', AUTOR(A): " + livroAutor + ", LOCAL: " + livroLocal
		 * + ";"; listaOrdenadaDeLivros.add(informacoesDoLivro);
		 * Collections.sort(listaOrdenadaDeLivros, new OrdenarLivroPorResumo());
		 * } return listaOrdenadaDeLivros;
		 
	}*/
	
}
